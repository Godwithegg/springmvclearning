<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 全局setting配置，按需再加 -->

	<!-- 配置别名 -->
	<typeAliases>
		<package name="com.danhuang.ssm.po"/>
	</typeAliases>
	
	<!-- 配置mapper,由于使用springmvc和mybatis整合，这里不需要配置，不过必须遵循mapper.xmljava文件同名，且在一个目录 -->
	<!-- <mappers></mappers> -->
	
	
</configuration>